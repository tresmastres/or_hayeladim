# Copia este archivo a .env y ajusta valores
# Base de datos (Render PostgreSQL: usa la Database URL que te da Render)
DATABASE_URL=postgresql+psycopg://usuario:contraseña@host:5432/nombredb

# JWT
JWT_SECRET=cambia_esto_por_un_secreto_largo
JWT_ALG=HS256
JWT_EXPIRE_MINUTES=60

# Stripe (opcional)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Entorno
APP_ENV=prod

# Datos emisor para facturas PDF
INVOICE_ISSUER_NAME=Mi Comunidad
INVOICE_ISSUER_TAXID=B12345678
INVOICE_ISSUER_ADDRESS=Calle Mayor 1, Madrid

# Email SMTP
SMTP_HOST=smtp.tu_proveedor.com
SMTP_PORT=587
SMTP_USER=usuario
SMTP_PASSWORD=clave
SMTP_FROM=Comunidad <noreply@tu-dominio.org>
SMTP_TLS=true
